// === GENERATED BY build/generate.sh — DO NOT EDIT BY HAND ===

variable "REGISTRY" { default = "ghcr.io/local" }
variable "IMAGE_NAME" { default = "personal-server" }
variable "TAG" { default = "dev" }

group "default" {
  targets = ["final"]
}

group "services" {
  targets = [__SERVICE_TARGET_LIST__]
}

target "_common" {
  context    = "."
  dockerfile = "build/Dockerfile"
  cache-from = ["type=registry,ref=${REGISTRY}/${IMAGE_NAME}:buildcache"]
  cache-to   = ["type=registry,ref=${REGISTRY}/${IMAGE_NAME}:buildcache,mode=max"]
}

__SERVICE_TARGETS__

target "final" {
  inherits = ["_common"]
  target   = "final"
  tags     = ["${REGISTRY}/${IMAGE_NAME}:${TAG}"]
  output   = ["type=docker"]
}
